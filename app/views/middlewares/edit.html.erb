<h2>Editing middleware</h2>

<%= form_for @middleware do |f| %>
  <fieldset>
    <%= render "middleware_form", :f => f %>
    <p class="buttons">
      <%= submit_tag "Update" %>
    </p>
  </fieldset>
<% end =%>
